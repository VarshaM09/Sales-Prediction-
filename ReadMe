Developed a sales prediction model using the Big Mart Sales dataset, involving data exploration, preprocessing,
and training of machine learning algorithms (XGBoost). Utilized Python and data science libraries including pandas,numpy and matplotlib. 
Evaluated model performance to select the best predictive Output
